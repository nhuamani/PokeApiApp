<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PokeApiApp" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue_500</item>
        <item name="colorPrimaryVariant">@color/blue_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/yellow_500</item>
        <item name="colorSecondaryVariant">@color/yellow_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:navigationBarColor">@color/blue_500</item>
    </style>

    <style name="SplashScreenTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Sta. -->
        <!-- Cambiar color de statusBarColor -->
        <item name="android:statusBarColor">@color/blue_700</item>
        <!-- Por default colorPrimaryDark es el color del StatusBar -->
        <item name="colorPrimaryDark">@color/blue_700</item>
        <!-- Cambiar color de navigationBarColor  -->
        <item name="android:navigationBarColor">@color/blue_700</item>

        <!--item name="android:windowBackground">@color/yellow_500</item-->
        <item name="android:colorBackground">@color/blue_700</item>
    </style>
</resources>